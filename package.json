{
  "name": "assim-telegram-bot",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build:clean": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "shell": "zsh && exit 0",
    "start": "node dist/index.js",
    "launch": "npm run build:clean && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/uuid": "^3.4.6",
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "ngrok": "^3.2.5",
    "sugar-env": "^1.5.7",
    "telegraf": "^3.34.1",
    "uuid": "^3.3.3"
  }
}
